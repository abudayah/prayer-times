# OS
.DS_Store

# IDE
/.idea
/.vscode

# Environment — never commit secrets
.env

# Composer dependencies — committed for reliable deployment
# /vendor/

# composer.phar is committed intentionally as a fallback for cPanel deployment

# User uploads — managed at runtime
uploads/*
!uploads/.gitkeep

# Temp
/tmp/
